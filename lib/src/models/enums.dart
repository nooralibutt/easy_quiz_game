enum QuizPlacement { menu }

enum QuizEventAction {
  play,
  levelFailed,
  levelComplete,
  continueWithGems,
  useExtraLife
}

enum QuizDifficulty { beginner, easy, medium, hard, expert }

enum QuizQuestionType { text, image }
